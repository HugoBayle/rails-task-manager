 <h1>My Tasks</h1>

  <% @tasks.each do |task| %>
  <div>
      <h2><%= link_to task.title, task_path(task) %></h2>
      <p><%= task.details %></p>
      <p><%= task.completed %></p>
  </div>
  <%end%>

<p><%= link_to "Add a new task", new_path %></p>
